version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_PORT=7860
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    volumes:
      # Mount only the artifact directory. Mounting /app/model shadowed
      # the package's code with stale volume contents.
      - model_data:/app/model/artifacts

  # Development service with hot reload
  dev:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
      - pip_cache:/root/.cache/pip
    ports:
      - "7860:7860"
    command: >
      sh -c "pip install -r requirements.txt &&
             streamlit run app.py --server.port=7860 --server.address=0.0.0.0 --server.runOnSave=true"
    profiles:
      - dev

volumes:
  model_data:
  pip_cache:
